interface Breakpoints {
    [key: string]: number;
}

export const breakpoints: Breakpoints = {
    lg: 1200,
    md: 768,
    sm: 0,
};

interface RowHeights {
    [key: string]: number;
}

export const rowHeights: RowHeights = {
    lg: 280,
    md: 180,
    sm: 164,
};

export const cols = { lg: 4, md: 4, sm: 2 };
